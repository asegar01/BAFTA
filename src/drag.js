import Game from './game.js';
import Scene from './scene';

export default class Drag extends Phaser.Scene {

    constructor()
    {
        super({key: 'drag'});
    }

    create()
    {
        
    }

    
}